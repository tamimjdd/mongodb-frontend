// Leave empty when the API is served from the same origin (local `npm start`).
// When the frontend is hosted separately (Vercel, Netlify), point this at the
// deployed backend, e.g. 'https://your-service.onrender.com'.
window.API_BASE_URL = 'https://mongodb-backend-t20a.onrender.com';
